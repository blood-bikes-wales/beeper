THREE_RINGS_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# GCP project ID used when connecting to live Cloud Datastore (npm run run).
# Not needed for local emulator dev (npm run dev sets this automatically).
GCP_PROJECT_ID=

# These are set automatically by `npm run dev` and should NOT be in .env.
# Uncomment and set manually only if you need to override the emulator host.
# DATASTORE_EMULATOR_HOST=localhost:8081
# DATASTORE_PROJECT_ID=beeper-local